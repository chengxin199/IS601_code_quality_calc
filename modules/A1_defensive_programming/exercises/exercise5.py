"""Exercise 5 (starter): replace sentinel return values with exceptions.

Students: implement `safe_lookup(mapping, key)` which returns mapping[key] or raises
KeyError if not found (instead of returning None or sentinel).
"""

def safe_lookup(mapping, key):
    raise NotImplementedError("Implement safe_lookup to raise on missing keys")
