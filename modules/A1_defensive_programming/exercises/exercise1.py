"""Exercise 1 (starter): replace nested conditionals with guard clauses.

Students: implement `safe_parse_and_add(s)` below. The tests in
`modules/A1_defensive_programming/tests/test_exercise1.py` will grade
your implementation.

Hints:
- Use guard clauses (early returns / raises) instead of nested if-else.
- Validate format and operand types and raise ValueError with helpful messages.
"""

from typing import Tuple


def safe_parse_and_add(s: str) -> float:
    """Parse a string like "add 2 3" and return 5.0.

    Replace this stub with a proper implementation.
    """
    raise NotImplementedError("Implement safe_parse_and_add: parse input, validate, and return the sum as float")

